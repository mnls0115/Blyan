# GPU Node Requirements
# Install with UV (much faster than pip):
#   curl -LsSf https://astral.sh/uv/install.sh | sh
#   uv pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#   uv pip install -r requirements-gpu.txt
# 
# Or with pip (slower):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements-gpu.txt

# Core dependencies for GPU nodes
aiohttp
aiohttp-cors
httpx
pyyaml
structlog

# Transformers and ML (Qwen3-30B requires transformers>=4.51.0)
transformers>=4.51.0
huggingface-hub>=0.25.2
safetensors>=0.4.3
accelerate>=0.34.0
tokenizers>=0.19.1

# Model optimization (optional, for quantization support)
# bitsandbytes>=0.41.0  # Optional: for INT8/INT4 quantization
# einops>=0.7.0  # For some model architectures
# sentencepiece>=0.1.99  # For some tokenizers

# Numerical computing
numpy
scipy

# Monitoring and utilities
psutil
pydantic
python-dotenv
cachetools>=5.3.2

# Blockchain/crypto (for node registration)
ecdsa
cryptography
base58>=2.1.0  # For IPFS CID validation

# Data handling
tiktoken
datasets

# Performance optimization (optional)
# flash-attn>=2.3.0  # Requires specific CUDA versions
# triton>=2.1.0  # For optimized kernels

# Development tools (optional)
# ipython>=8.12.0
# jupyter>=1.0.0
# matplotlib>=3.7.0

# Common ML utilities
tqdm
regex
filelock
packaging

# For distributed training (optional)
deepspeed
fairscale