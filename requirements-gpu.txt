# GPU Node Requirements
# Install with:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements-gpu.txt

# Core dependencies for GPU nodes
aiohttp
httpx
pyyaml
structlog

# Transformers and ML
transformers==4.44.2
huggingface-hub>=0.25.2
safetensors>=0.4.3
accelerate>=0.34.0
tokenizers==0.19.1

# Model optimization (optional, for quantization support)
# bitsandbytes>=0.41.0  # Optional: for INT8/INT4 quantization
# einops>=0.7.0  # For some model architectures
# sentencepiece>=0.1.99  # For some tokenizers

# Numerical computing
numpy
scipy

# Monitoring and utilities
psutil
pydantic
python-dotenv

# Blockchain/crypto (for node registration)
ecdsa
cryptography

# Data handling
tiktoken
datasets

# Performance optimization (optional)
# flash-attn>=2.3.0  # Requires specific CUDA versions
# triton>=2.1.0  # For optimized kernels

# Development tools (optional)
# ipython>=8.12.0
# jupyter>=1.0.0
# matplotlib>=3.7.0

# Common ML utilities
tqdm
regex
filelock
packaging

# For distributed training (optional)
deepspeed
fairscale